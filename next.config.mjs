/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // View Transitions API gebruiken we client-side waar mogelijk; geen extra flags nodig.
};

export default nextConfig;
